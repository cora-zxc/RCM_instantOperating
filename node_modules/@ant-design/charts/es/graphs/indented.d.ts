import React from 'react';
import { LabelStyle } from '@antv/g6';
import { RelationGraph } from './types';
export interface IndentedTreeProps extends RelationGraph {
    /** 全局 title 样式 */
    titleStyle?: LabelStyle;
    /** 全局 body 样式 */
    bodyStyle?: LabelStyle;
    /** 全局 footer 样式 */
    footerStyle?: LabelStyle;
    /** 全局 footer value 样式 */
    footerValueStyle?: LabelStyle;
    /** 是否展示尾部箭头，默认 true */
    showArrow?: boolean;
}
declare const IndentedTreeGraph: React.FC<IndentedTreeProps>;
export default IndentedTreeGraph;
